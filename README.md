# IARDISPLAY

I Am Responding bay display application, written in Golang.

This allows "smart" TVs to be pointed at a particular URL and automatically display read-only data about an agency's response without having to manually enter credentials every time a monitor is reset.

